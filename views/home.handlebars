<section id="post" class="container">
  {{#each posts}}
    {{! <div class="card bg-light mb-3" style="max-width: 18rem;">
      <div class="card-header" class="d-flex justify-content-start">
        <h5>Title</h5>
        <div>
                  <p class="d-flex justify-content-end" class="card-text"><small
            class="text-muted"
          >Posted by: </small></p>
        </div>

      </div>
      <div class="card-body">
        <p class="card-text">Some quick example text to build on the card title
          and make up the bulk of the card's content.</p>
      </div>
    </div> }}
    <div class="card-deck">
      <div class="card">
        <div class="card-header">
          <h5 class="card-title">{{title}}</h5>
        </div>
        <div class="card-body">
          <p class="card-text">{{body}}</p>
          <p class="card-text"><small class="text-muted">Posted by: {{user.username}} at {{createdAt}}</small></p> {{!how do i get the user id to show the name}}
        </div>
      </div>
    </div>
  {{/each}}
</section>